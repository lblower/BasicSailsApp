<!DOCTYPE html>
<html>
  <head>

    <title><%=typeof title == 'undefined' ? 'Fixed Header' : title%></title>

    <!-- Viewport mobile tag for sensible mobile support -->
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <!--STYLES-->
    <link rel="stylesheet" href="/styles/importer.css">
    <link rel="stylesheet" href="/bootstrap/dist/css/bootstrap.min.css">
    <!--STYLES END-->
  </head>
  <body>

    <nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Basic Sails Crud operations</a>
    </div>
    <ul class="nav navbar-nav   float-right">
          <% if(!session.authenticated) { %>
      <li class="active"><a href="/">Login</a></li>
      <li><a href="/signin">Register</a></li>
      <% } %>

      <% if(session.authenticated) { %>
        <% if(session.user && session.user.admin) { %>
      <li><a href="/alluser">View All</a></li>
      <% } %>
  <li><a href="/user/<%= session.user.id %>">My Profile</a></li>

      <% } %>

    </ul>
    <% if(session.authenticated) { %>

    <ul class="nav navbar-nav navbar-right">
    <li><a href="/user/<%= session.user.id %> "><span class="glyphicon glyphicon-user"></span> <%= session.user.firstname %> </a></li>
    <li><a href="/logout"><span class="glyphicon glyphicon-log-in"></span> Logout</a></li>
  </ul>
<% } %>

  </div>
</nav>


    <div class="jumbotron">
      <div Class='container'><h1>Welcome to Sails Js</h1>
      <p class="alert-success">In this part we are going to just build simple crud operations</p>
      </div>
    </div>
